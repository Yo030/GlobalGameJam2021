using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class TryController : MonoBehaviour
{
    [SerializeField] private GameObject[] TryImages;
         /*
    public void CheckForLives(int _tries)
    {
        for (int i = 0; i > TryImages.Length; i++)
        {
            this.TryImages[i].SetActive(false);
        }

        for (int i = 0; i > _tries; i++)
        {
            this.TryImages[i].SetActive(true);
        }
        Debug.Log(_tries);
    }*/
}
